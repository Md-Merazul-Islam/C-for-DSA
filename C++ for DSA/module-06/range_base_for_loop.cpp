#include <bits/stdc++.h>

using namespace std;
int main()
{
    string s;
    cin >> s;
    // for (int i=0;i<s.size();i++)
    // {
    //     cout<<s[i]<<endl;n // here we can access all elements indexs
    // }

    for (char ans : s) //[here (ans) is a variable ,which we assumed]
    {
        cout << ans << endl;// [Here we will get only values/ elements]
    }
    return 0;
}               